import { gql } from "@apollo/client";

// Phase 1: GraphQL queries (stubs)
// Phase 2: Will add actual queries for tasks, user, etc.

// Example query structure (commented out until backend is ready)
/*
export const GET_TASKS = gql`
  query GetTasks {
    tasks {
      id
      title
      description
      status
      priority
      dueDate
      createdAt
      updatedAt
    }
  }
`;

export const GET_TASK = gql`
  query GetTask($id: ID!) {
    task(id: $id) {
      id
      title
      description
      status
      priority
      dueDate
      createdAt
      updatedAt
    }
  }
`;

export const GET_ME = gql`
  query GetMe {
    me {
      id
      email
      createdAt
    }
  }
`;
*/
